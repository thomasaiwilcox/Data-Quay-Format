[package]
name = "qf-validate"
version = "0.1.0"
edition = "2021"
description = "CLI validator for Quay Format (QF) v1.0 files"
license = "MIT"

[[bin]]
name = "qf-validate"
path = "src/main.rs"

[dependencies]
qf-core = { path = "../qf-core" }
